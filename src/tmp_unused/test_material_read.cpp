#include <iostream>
#include <fstream>

#include <string>

#include <catch2/catch.hpp>

#include "../material.h"

using namespace IcarusPyro;

TEST_CASE("1: Read YAML database entries for a material.", "[io]") {

    // std::string database = "tacot.yaml";
    // Material TACOT(database);

    // std::string material_name("tacot");
    // REQUIRE(TACOT.get_name() == material_name);
}
